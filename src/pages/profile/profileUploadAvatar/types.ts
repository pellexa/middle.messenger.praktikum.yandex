import Button from '../../../components/button'
import { TagAttribute, ComponentEvent } from '../../../modules/types'

export type ProfileUploadAvatarProps = {
  tagAttrs?: TagAttribute
  acceptButton: Button
  events?: ComponentEvent
}
